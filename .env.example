# Telegram bot token from @BotFather
BOT_TOKEN=

# Your bot's username WITHOUT the @ (e.g. MakytonBot)
BOT_USERNAME=

# Postgres connection string (Railway provides this automatically when you add a Postgres plugin)
DATABASE_URL=

# Set to "true" if your Postgres provider requires SSL (Railway's public DB URL usually does)
PGSSL=true

# --- Chat IDs ---
# Makyton Verification Group: where wallet submissions + task completion logs are posted.
# Get this by adding the bot to the group as admin and using a tool like @userinfobot,
# or temporarily logging ctx.chat.id when the bot receives a message there.
MVG_CHAT_ID=

# Main public group: where the welcome message + "has become a Makytian" announcement
# + /stats /refcode commands are used. Also counts as the "join group" task target.
MAIN_GROUP_ID=

# Optional: a channel users must join for the "join channel" task.
# Leave blank if you're only using the main group for this task.
TARGET_CHANNEL_ID=
TARGET_CHANNEL_USERNAME=

# --- Reward amounts (in $MYT) ---
REWARD_JOIN_TASK=15
REWARD_START_BOT_TASK=10
REWARD_REFERRAL=10
